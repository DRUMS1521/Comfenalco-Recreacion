# En desarrollo: vacío -> la app usa el proxy de Vite (/api -> :8000), lo que
# permite abrirla desde localhost o desde la IP de la red sin problemas de CORS.
# En producción: URL completa del backend.
VITE_API_URL=
